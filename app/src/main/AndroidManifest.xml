<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Quyền đọc ảnh: Android 13+ dùng READ_MEDIA_IMAGES, Android 12- dùng READ_EXTERNAL_STORAGE -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.Light.NoActionBar">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaS" />

        <!-- Launcher -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Dashboard -->
        <activity android:name=".ui.DashboardActivity" android:exported="false" />

        <!-- Nhân viên -->
        <activity android:name=".ui.EmployeeActivity" android:exported="false" />
        <activity android:name=".ui.AddEmployeeActivity" android:exported="false" />
        <activity android:name=".ui.EditEmployeeActivity" android:exported="false" />

        <!-- Phòng ban -->
        <activity android:name=".ui.DepartmentActivity" android:exported="false" />

        <!-- Tài khoản (Admin) -->
        <activity android:name=".ui.AccountManagementActivity" android:exported="false" />

        <!-- Chấm công -->
        <activity android:name=".ui.TimekeepingActivity" android:exported="false" />
        <activity android:name=".ui.GPSCheckInActivity" android:exported="false" />

        <!-- Công việc -->
        <activity android:name=".ui.TaskActivity" android:exported="false" />

        <!-- Thông báo -->
        <activity android:name=".ui.NotificationCenterActivity" android:exported="false" />
        <activity android:name=".ui.CreateNotificationActivity" android:exported="false" />

        <!-- Đơn từ -->
        <activity android:name=".ui.LeaveApplicationActivity" android:exported="false" />

        <!-- Chat -->
        <activity android:name=".ui.ChatListActivity" android:exported="false" />
        <activity android:name=".ui.ChatRoomActivity" android:exported="false" />

        <!-- Cá nhân -->
        <activity android:name=".ui.ProfileActivity" android:exported="false" />
        <activity android:name=".ui.EditProfileActivity" android:exported="false" />
        <activity android:name=".ui.ChangePasswordActivity" android:exported="false" />

    </application>

</manifest>